extends: ../base.yaml

experiment:
  model_tag: qwen25-05b
  method: grpo
  train_prompt_mode: zero-shot
  variant: none
  kind: train

training:
  max_steps: 2000
  learning_rate: 3.0e-6
  warmup_steps: 200

sft_pretrain:
  enabled: false

retrieval:
  enabled: false

wandb:
  run_name: grpo-zero
